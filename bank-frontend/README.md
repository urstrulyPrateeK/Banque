# Banque Frontend

Angular 21 frontend for Banque.

## Run
`npm ci && npm start`

## Build
`npm run build`

## Notes
- UI routes are served directly in development and through Spring Boot in the single-service deployment.
- API base URL is configured in `src/environments`.
